1. template literal
    `${a}${b}`:문자열을 만드는것

2.Number()함수
    문자열을 숫자로 변환

3.Math.max()함수
    주어진 함수중에 가장큰값

4.+ 연산자는 불리언 값을 숫자로 변환하는 역할을 합니다.
true는 1로, false는 0으로 변환됩니다.
function solution(str1, str2) {
    return +str2.includes(str1);
}

5.indexOf():
indexOf() 메서드는 대상 문자열에서 지정된 문자열이 발견된 위치(인덱스)를 반환합니다.
문자열이 발견되지 않으면 -1을 반환합니다.
대소문자를 구분합니다.
ES5에서부터 사용 가능한 메서드입니다.

6.getFullYear()은 JavaScript의 내장된 Date 객체의 메서드 중 하나입니다.
이 메서드는 해당 Date 객체가 나타내는 연도를 반환합니다.
=Date().getFullYear()

7.Math.floor() 함수를 사용하여 소수점 이하를 버린 후 정수를 얻을 수 있습니다.

8.parseInt() 함수를 사용하여 문자열을 정수로 변환할 수 있습니다.
이 함수는 문자열을 파싱하고 정수 부분만 반환합니다.

9.반복 가능한(iterable) 객체는 여러 개의 원소를 포함하고 있으며,
이러한 원소들에 대해 반복적으로 작업할 수 있는 객체입니다.
Python에서 반복 가능한 객체는 다음과 같은 특징을 가지고 있습니다
1.list:순서가 있는 데이터 집합으로 각요소에는 인덱스로 지정되어있습니다
2.tuple: 튜플은 리스트와 유사하지만 불변순서가 있는 데이터 집합
3.string: 문자열은 문자의 시퀀스이면, 각문자에 인덱스가 지정되어있다
4.set: 세트는 고유한 요소들의 집합으로 순서가 없다
5.dictionary:딕셔너리는 키-값 쌍의 집합으로 각키를 사용하여 해당값찾기가능

10.range(start,stop,step)

11.<< 더블비트 시프트 :

12.var:재선언 재할당가능//let:재선언불가,재할당가능//const:재선언,재할당불가
var i =10;
for(var i=0;i<5;i++){
    console.log(i);
}
console.log(i);//5 다음 수치가 반영, 4에서 한번더 증가시켜줌//재선언함으로써 10이라는 값이 사라짐

console.log("===============");
const a = 10;
console.log(a);
// const a = 20;에러남
// console.log(a);
console.log("===============");

let j = 10;
for(let j=0;j<5;j++){
    console.log(j);
}
console.log(j);//10 전역변수

13 this 키워드
-혼자 써잇다면 전역객체를 의미
-함수에서는 전역객체를 의미
-메서드내부에서는 메서드를 소유한 객체를 의미
-이벤트는 이벤트를 받는 객체의미
