//리턴뒤에 어떤코드를 입력해도 실행되지않은 리턴은 끝을 의미 
// 리던튼 콜솔 로그터러  결과값을 반환할때  그자리에 쓴다 



// x와t를 합하기
function calcSum(x,y) {
    // let result = x + y;
    // return result;
    return x + y ;
    }
    //빼기/
    function calcSubs(x,y) {
        return  x - y;
        }

//곱하기
function calcmMulti(x,y) {
    return  x * y;
    }

    //나누기
    function calcSum(x,y) {
        return  x / y;
        }


    
let firstNum= Number(prompt)
let secondNum= Number(prompt)
let signal= Number(prompt ("1-더하기, 2-빼기 "))