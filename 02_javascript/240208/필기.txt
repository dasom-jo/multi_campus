240208

다른화면으로 이동시 중앙으로 켜지는 점
섹션에 아이디 쓰고 에이태그에 섹션아이디 넣기 

바디에 디스플래이 플랙스 넣으면 가로로 넣은거라
여백이 많아질수잇음

  on이용  off



파일명에 공백 안넣기


자바스크립

undefined 결과값이 없을때


html먼저 해석하다가 자바 나오면 자바 해석ㄷ함
구조가 중요함

방향ㅋ위로하면 전에쓴거 나옴

자바에 clg/log 치면console.log(object); 바로 뜸

변수:값을 저장할수잇는 메모리 공간에 붙은 이름 /컨테이너 

네이밍컨벤션:소문자 대문자일지 약속을함 소문자씀/
-camelcase:두번째 단어가 대문자
-snake_case:모두소문자 대신 띄어쓰기는 언더바

변수명규칙
-숫자로 시작할수없다
-문자,숫자,$,_만 사용가능
-이름안에 공백 불포함
-대소문자를 구분
-예약어는 사용할수없다 ex:class 캡쳐해놈
 -무의미한 변수이름은 피한다


let 끼우면 값 선언:최초로 선언되는 변수에 붙이는 습관을 들인다면 이미 선언된 변수가 
변경되는것을 방지가능(var 범위는 다르지만 비슷란 선언 변수)
let name = 손흥민;
const=상수취급

자료형 
프로그램에서 처리하는 자료형태 /기본형(number,srting,boolean,null,undefined)과 객체형(funstiom,array,정규표현식)구분

숫자 자료형
정수와실수
숫자라고하더라도 따음표로 묶이면 문자열로 인식

remainder=7%2 (나머지)

infinty? nan?
 
Infinity:무한대의 값 
nan:not a number 

문자자료형 "'`으로 묶인 문자자료

프로그래밍할때 폰트 정해저잇음  ` 이랑 ' 이 구분 안가면안됌


불링자료형 true fail
 
undefined:변수를 선언하기만하고 값을 할당하지않앗을때/대신값이면서동시에 자료형
not-defined: 비선언

null 유효하지않은값,. 하지만 역시 값이면서 동시에 자료형

문자와 숫자는 +만 가능 
숫자를 가르키는 문자열은 *-가능 
 

배열: 하나의 변수에 여러 값을 할당할수있는 형태
 빈배열/문자열배열/여러자료형으로 구분된배열

==:값비교 
===:자료형까지 비교

자동형변환:연산을통해 자료형이변화 ;Number() string() boolean()
강제형변환:직접자료변환


연산자 
++ 1증가
-- 1감소

단항 -산술-비교-논리-할당

if (조건){
조건1이 참일경우 ,실행될 코드}
else if (조건2){
조건 2가 참일경, 시랭될코드
else

인풋아래  script





