# 친절한 함수 만들기
def plus(a:int, b:int) -> int:
    """두 개의 정수를 합쳐서 반환한다.

    Args:
        a (int): 첫 번째 정수야
        b (int): 두 번째 정수야

    Returns:
        int: 합한 값을 반환한다.
    """
    return a + b

help(plus)