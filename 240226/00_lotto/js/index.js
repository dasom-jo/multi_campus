const result = document.querySelector('#result');
const button = document.querySelector('button');

const luckyNumber = {
    digitCount: 6, // 숫자 6개
    maxNumber: 45, // 최대 숫자 45
};

// 버튼 클릭 시, 로또번호 추출하여 출력
button.addEventListener('click',()=>{
    const {digitCount,maxNumber} = luckyNumber;
    //clg
    /*const numberSet = new set();
    for (let i =0; i<digitCount; i++){
    let randomNuber = Math.floor(Math.random() * maxNumber) +1;
    numberSet.add(randomNuber);  
    }*/ //내림정수로 바꿔주고 숫자 는 0~1사이에서 오ㄴ니깐  맥스넘저정의해놈 45 이키타운트도 정의해놈 6

    count 
}); 

console.log(numberSet); 